# Resumed R2 game validation and storage cleanup — 2026-09-14

This report continues [the earlier autonomous investigation](fps-autonomous-validation-20260914.md). That report records the earlier stopping point; its unrun statuses and retained-backup paths are historical. The user subsequently authorized continued autonomous investigation and deletion of unwanted YAAGL logs and obsolete YAAGL/legacy backups. Working installation activation remains outside this task.

Evidence root E is `/Users/david/Library/Application Support/YAAGL Local Builds/fps-autonomous-validation-20260914T130005Z`. Resumed evidence N is `E/resume-20260914T182019Z`. Original receipts remain unchanged; new deletion receipts explain which old paths were removed or replaced by verified lossless archives.

## Build and runtime actually exercised

The checkout was clean at `9fa5faabfdf9dfed9076306a028f50ce9ebcb3d6` when work resumed. The actual packaged app remains `E/package/Yaagl OS.app`, built from `4e396b1cc4f2e1bc4637b8a44a75752ee9a71c57`. Later report-only commits do not change that binding. A fresh audit verified all 511 packaged files, 103,631,235 bytes, path sets, modes and hashes, including 16 ASAR members and 19 Sophon files.

Tests invoke its actual `Contents/MacOS/Yaagl` with the independent `E/profiles/candidate` profile and its own Wine/prefix. The app wrapper's default `Yaagl OS FPS Review owned3` profile is not used. The selected candidate is `E/profiles/candidate/wine`; the similarly named, unselected runtime copies have been deleted. No installed or development Wine was replaced.

| Artifact                                 | SHA-256                                                            |
| ---------------------------------------- | ------------------------------------------------------------------ |
| Packaged ARM64 native, owned3            | `c67e7eabd572ca7c96dd1ab547a67faa0bac8e08e4fb50e95dd1850af6571939` |
| Packaged resources.neu                   | `99e6f56cccb348b7f23cdbbc78b0c1298c77ae393b403edcd9c430dae4753a4d` |
| Bridge protocol 3, 41,984 bytes          | `59ae7f9e1b753499386cfe5336c9f4a4d911c39c69ce3447712c4522ccbe7c95` |
| Unchanged signed Steam64 shim            | `0424339444c54bf1f9fdbadf12e4e2c90ceef41d987fe573b93f5f2ebfd8a657` |
| Selected R2 ntdll.so, 620,688 bytes      | `eef64f611ae9033261a70f46ec0be38d58823717f14e80331946c6d0cd3c85f7` |
| Independently retained original ntdll.so | `f26ade35f5b49e33b3780b6adc71f9eb9c831ea40222c1bae667ac14135d984b` |

Actual game 7.0.0 is `/Users/david/.gimpact/GenshinImpact.exe`, SHA-256 `a1a23cb76d941df28c5156ca3152fa49421ec98842221b7d71633d42ee76ca45`. MHYPBase SHA-256 is `cdc79ffdb73bd2e157c7faee33740f84aa7aec845c8cb4d943f8fb4dff29d01e`. Steam Patch ON, Launch Fix/block hosts OFF, Timeout Fix ON, Metal HUD ON and DXMT 0.80.0 were retained. These runs do not establish real-game Launch Fix compatibility.

The app requires macOS 14+, has an ad-hoc signed ARM64 native executable and an unsigned, unnotarized outer bundle. Intel helpers require Rosetta. Sophon resources, manifests and licenses are complete, and actual packaged UI health passed. Existing Intel xdelta still requires the unavailable `/usr/local/opt/xz/lib/liblzma.5.dylib`, limiting legacy xdelta updates. The app contains no Wine, private profile or game data and does not install R2.

## Observed runs and limits

| Configuration                             | Game and worker evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Cleanup and restoration                                                                                                                                                                                                                                                                                                |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Earlier original Wine, FPS OFF, stored 60 | Stable login with HUD 60.00. No FPS worker expected. This earlier control is preserved; it was not repeated.                                                                                                                                                                                                                                                                                                                                                                                                  | Ordinary game UI exit, launcher cleanup acknowledgement, native exit 0 and independent 28 path states/two typed registry values match. Windows game exit code was not directly captured.                                                                                                                               |
| R2 enabled 60                             | Request `9353b70a506bcaf9e7c9554d0cf96a40b9ac15f4ba6ea7866eb4ebd347b9ed87`, `game_1789410846180.log`, Windows game PID 280. Worker generation 1; 1,034 successful reads, one successful four-byte write from -1 to 60, 1,033 compare-equal reads. Stable login HUD 60.00.                                                                                                                                                                                                                                     | Game exit 0 at 18:37:56.826 UTC; worker done, empty jobs, release, supervisor completion, both Wine waits and registry/file cleanup at 18:38:06.084. Native exit 0 at 18:39:33.554. Fresh 28+2 comparison passes; the missing-parent coverage qualification below remains.                                             |
| R2 enabled 120                            | Request `9eeaf7f630f21287836b3f8786d4b97ec4d599b4990a4307d576227f8e0a9d83`, `game_1789411732978.log`, Windows game PID 280. Worker generation 1; final heartbeat records 4,494 successful reads, five successful four-byte writes and 4,489 compare-equal reads. Nine in-world video samples over 240 seconds record 116.85–118.33 FPS (median 117.71); each exceeds 60.                                                                                                                                      | Game exit 0 at 19:04:10.980 UTC, cleanup at 19:04:20.254, native exit 0 at 19:05:09.744. Fresh 29+2 comparison and strict coverage of every journal path and both native registry snapshots pass.                                                                                                                      |
| R2 enabled 150                            | Request `9b7deecd36a51a45505443b91f6f1e3a5eb5a40016fa528510039c568adb2e9f`, `game_1789412982331.log`, Windows PID 280. Worker generation 1; 3,792 successful reads, four successful four-byte writes, 3,788 compare-equal reads. A 600-second observation after world entry includes normal movement, graphics/map menus and one fast-travel transition. Thirteen world screenshots record 109.91–120.98 FPS (median 114.69) over 422 seconds; two map samples are excluded. The continuous recording failed. | Ordinary WM_CLOSE at the planned deadline; game exit 0 at 19:22:40.609 UTC, cleanup at 19:22:49.882, native exit 0 at 19:23:56.784. Fresh 29+2 comparison and strict journal/native-snapshot coverage pass. Planned full-video evidence was not captured; this does not turn normal game exit into a gameplay failure. |

The 60 run exceeded its planned three-minute/4 GB limit: normal exit was delayed, and its 180-second recording ended before quit. The final Wine stream is 4,696,237,035 bytes. The 120 run also exceeded the intended 600-second total launch budget because of controller delay. Its recording ended at 18:58:51.841; the game remained alive until 19:04:10.980. These are finite functional observations with controller/capture deviations, not strict passes of the planned time bounds or claims of uninterrupted video coverage through exit.

The 120 video review sampled 12 frames at PTS 240–570 seconds. Eleven numerical HUD readings range from 111.40 to 118.33 FPS (median 117.52); PTS 300 shows the desktop without a HUD, so the wider interval has an explicit interruption. The nine samples from PTS 330–570 span 240 seconds at 116.85–118.33 FPS (median 117.71), all above 60; inspected scene crops show the game world. This is sampled sustained above-60 evidence, not verification of every frame. Graphics/VSync settings were not captured in that run. `N/r2-enabled120/analysis/worker-cleanup-video-audit.md` and its JSON retain counters, measurements and qualifications. Its first generic monitor incorrectly reported zero log bytes after excluding an already-present stream; the exact-token bridge monitor supplies the correct stream and size. That initial review did not scan the full Wine trace; the later full-file diagnostic-pattern scans described below cover all three streams.

For 120, the visible Exit to Desktop interaction did not complete exit. An identity-checking helper then inspected the live game and posted one ordinary WM_CLOSE to its single visible ownerless window after checking the exact image, PID and creation time `01dd4479b47c1932`. Its inspect and close invocations added two Wine clients after observation. It did not terminate a process, release ownership or bypass launcher cleanup. Helper success is not the cleanup proof; the retained game exit and launcher acknowledgements are. At the end of both runs a VirtualQueryEx error 5 was recorded during normal exit; no FPS read/write failure followed. This is not classified as a worker error.

Run evidence is under `N/r2-enabled60`, `N/r2-enabled120` and `N/r2-enabled150`, with the exact launcher invocation and terminal logs, available recordings/screenshots, live/final collectors, request versions, final profile-root DXMT logs, preimages and independent restoration/coverage reports. Final acknowledgements and file equality are separate gates.

## Evidence capture correction

The enabled-60 prelaunch capture contained 28 path states. Strict journal coverage correctly rejected one missing entry: the existing `wine/lib/wine/x86_64-unix` parent, recursively journaled when `winemetal.so` was initially absent. The journal does not create, remove, chmod or chown that existing parent during restoration; it verifies its directory kind. Nevertheless, the fresh capture was incomplete.

The original 28-path manifest and strict failure remain unchanged. `N/r2-enabled60/analysis/historical-parent-supplement.json` separately compares the parent with independent metadata captured at 13:16:57 UTC and its post-run state. The parent kind, mode and ownership match, and the other fresh 28 path states and two typed registry values match. This is not a claim that 29 paths were freshly captured for that run; continuity was not monitored throughout the intervening hours.

The external evidence tool `E/tools/run-preimages.py` now captures 29 paths, including that parent. Two meaningful inert regressions fail on the original tool and pass after correction: a changed parent mode must fail restoration comparison, and the independently constructed recursive journal must have prelaunch coverage. Together with the existing suites, 14 distinct evidence tests pass. Old tooling and before/after results are retained in `N/evidence-tool-correction`; the strict coverage checker was not weakened. Enabled 120 uses the corrected tool and passes all 29 fresh path comparisons, two typed registry comparisons, all 28 actual journal paths and both native snapshots.

## Delayed-crash interpretation

The original target-150 crash still supplies the strongest Wine mechanism lead: the seventh successful four-byte FPS write and a fatal read of another value on the same page share one Wine millisecond. Error 87 is a later ReadProcessMemory failure, approximately two seconds after the game fault, rather than evidence of an invalid target. The earlier generation-0 startup crash is addressed by canonical Steam as Wine's initial process with desktop preparation before game creation; that mechanism is preserved.

The existing reporting correction retains primary worker/game failures separately from failures actually encountered during cleanup. Confirmed restoration does not suppress the original crash, and a primary worker error is not relabelled as a restoration failure. This resumed work has not added a new application crash correction or weakened that classification. The capture-tool correction is an evidence improvement, not a game fix.

R2 uses current protection rather than allocation protection to decide whether to toggle an executable page. Fresh source/binary comparison and 8,003 offline assertions support that correction. The original crash lacks a fault-time protection capture, so the exact interleaving is still not proven. R2 does not resolve every independent mapping/lifetime race or unchecked executable-protection failure. Finite candidate gameplay cannot establish that all delayed crashes are fixed. The separate Wine shutdown-thread abort remains distinct.

Full-file diagnostic-pattern scans covered all three unchanged Wine streams: 4,696,237,035 bytes at 60, 14,160,776,776 at 120 and 11,818,927,292 at 150. No c0000005 dispatch or unhandled marker matched those patterns; many handled syscall access faults remain in the sources. The 150 scan retained 36,762 matching lines, including a nested signal-stack exception on thread 0184 at Wine tick 13062.860 during normal-close teardown. The latter preceded the retained game exit 0 and successful cleanup and remains a separate shutdown finding, not evidence that the original gameplay access violation recurred. The scan is complete for its recorded patterns, not a semantic interpretation of every trace line. `N/r2-enabled150/analysis/complete-wine-diagnostic-scan.json`, matching-lines archive and shutdown context preserve the result. A scoped diagnostic-report search found no new 150 shutdown crash report; the discovered Wine report is an earlier disk-write microstackshot for the 120 process, with no action taken.

A separately discovered 15:40 UTC run from `~/Library/Application Support/Yaagl OS` used the legacy launcher and its selected per-game Wine, not this candidate. It records target 120 and legacy override operations but no numerical HUD, byte counts, readbacks, game exit code or modern restoration evidence. Actual selected ntdll SHA-256 is `1847d332d702d12b68d4ba9031a01508546a3e6cf6147bd3b0a0a21de4bb26c9`; direct disassembly finds the original allocation-protection mechanism despite the different whole-file hash. This is not another accepted R2 result or proof of a gameplay crash. See `N/newer-working-run-capture` and `N/legacy-active-ntdll-comparison`.

## Authorized storage cleanup

The agent completed cleanup rather than assigning it back to the user. In the first cleanup pass, ten unique full log streams were compressed losslessly, independently decompressed and checked against full-byte SHA-256 hashes. Thirty-seven duplicate/plaintext references were deleted only after each removed source was rehashed and matched to its archive. This includes the original disabled control and original delayed-crash trace. Their old plaintext paths no longer exist; the complete decoded evidence remains recoverable. `N/storage/LOG-DELETIONS.jsonl` maps each old source to its archive, exact byte length and hash. `LOG-COMPACTION-SUMMARY.json` records free space rising from 25,906,651,136 to 206,975,639,552 bytes during this pruning step, a gain of 181,068,988,416 bytes before subsequent tests generated new evidence.

Nine superseded app bundles, three unselected runtime copies and the completed isolated baseline profile were also removed. Compact package manifests, inventories, baseline final registry/configuration/DXMT state and deletion receipts remain under `N/storage/removed-backup-records`, `BACKUP-DELETE-PLAN.json` and `BACKUP-DELETIONS.jsonl`. The baseline profile and unselected runtime paths in the older report are no longer usable launch targets.

Explicitly obsolete legacy backups were removed as well: `wine.original-before-fixed-fullscreen` and its adjacent `wine.tar.xz`, plus the legacy fullscreen distribution archive. Exact entries are in `N/storage/LEGACY-BACKUP-DELETIONS.jsonl`. Historical rollback machinery relying on those removed backup paths is no longer usable. The active legacy `dist/wine`, which is a live symlink target, remains intact; its identity was checked after deletion. Source repositories/history, dependencies, lockfiles, selected candidate, current app, installed/development Wine and shared game data were preserved. Symlinks were unlinked without traversing their targets. No retained request or restoration guard was removed.

After all test processes and raw-log readers exited, the three resumed Wine streams were also archived with full decoded-byte and live-prefix verification. Nine more raw/live/final copies were deleted. `N/storage/completed-current-logs-20260914T193300Z/CURRENT-LOG-DELETIONS.jsonl` maps these removed paths; adjacent archives preserve every byte. Its summary records another 29,176,832,000-byte free-space gain. Across both batches, 46 raw log references were removed and 13 complete streams remain losslessly archived. Final free space is about 197 GB, versus 26.9 GB at resume (about 170 GB net more free, after generating the new test evidence). No source manifest was rewritten to hide the removed raw paths.

## Validation status

The unchanged app source retains its pinned Node 16.20.2/pnpm 7.33.7 results: 2,042 Vitest tests in 27 files; type and format checks pass; nine existing lint warnings, zero errors. Ten collector regressions, owned-supervisor fixtures, portable memory diagnostics with sanitizers and negative mutations, four actual frozen-timer native bootstrap fixtures and both R2 harmless bridge suites passed previously. The new evidence-tool regressions exercise isolated files and do not start the launcher, game or Wine.

During the 150 test, read-only settings inspection recorded Windowed 1512×982, Custom quality, menu FPS 60, VSync OFF, render resolution 0.6 and mostly Lowest/OFF visual settings. No settings were changed; the menu FPS choice is distinct from the worker target 150 and measured HUD FPS. See `N/r2-enabled150/graphics-observation.json`. This configuration limits extrapolation to higher graphics loads.

The 150 observation began at 19:12:38.713 UTC. Its identity-checked timer posted one ordinary WM_CLOSE at 19:22:38.724, approximately 11 ms after the planned 600-second deadline; exit followed at 19:22:40.609. Graphics/menu inspection, movement, map navigation and one ordinary fast-travel transition were included, so this is not ten uninterrupted minutes of rendered world. The final write at 19:11:49.440 preceded world observation; no later gameplay reset or write occurred. Continued successful reads and natural transitions therefore do not recreate the original delayed seventh-write timing. No extra writes or manufactured resets were introduced.

The 13 world HUD samples span 19:15:21.222–19:22:23.533 UTC (about 422 seconds), with two map samples excluded and natural transitions between world scenes. Every numerical world sample exceeds 60, ranging from 109.91 to 120.98 FPS with median 114.69. These are sampled observations, not continuous frame coverage or a claim of achieved 150 FPS. The recorded generation remained 1; final query error 5 and mappingChanges 1 arise from the observational teardown query, not a proved live protection change or failed read/write. Prior live samples show allocation 0x80/current 0x8 without sampled changes. See `N/r2-enabled150/analysis/worker-cleanup-fps-audit.md` and its JSON.

The 150 recording failed to finalize: SIGINT ended the recorder with child exit -2 and no `game.mov` remained. The outer wrapper tool returning 0 was initially misinterpreted; its original child-exit receipt is retained. A scoped temporary-file search found no recoverable recording. Fifteen periodic screenshots, additional world/transition screenshots, timer health records, full runtime logs and cleanup/restoration evidence remain. This is a finite functional result with screenshot evidence; the original planned full-recording gate was not met. The user subsequently declined further UI reviews, and the recording gap is not a reason for another mandatory run. No automatic game retry follows.

No further UI review or automatic runtime test is planned. The optional operator checkpoint allows ordinary target-150 gameplay in the same isolated candidate if the user wants to extend crash verification, retaining fresh logs and cleanup/restoration comparisons. A recording or appearance review is not required. This does not authorize working-profile activation or imply general crash-free acceptance.
